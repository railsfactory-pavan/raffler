<h1>Raffle</h1>

<div ng-controller="RaffleCtrl">
  <from>
    <input type="text" ng-model="newEntry.name">
  </from>

  <ul>
    <li ng-repeat="entry in entries">
      {{entry.name}}
    <li>
  </ul>
</div>
